---
// Subtle tech-inspired background pattern
// Neural network / blueprint style with very low opacity (<3%)
---

<div class="tech-bg" aria-hidden="true">
  <svg class="tech-pattern" preserveAspectRatio="none">
    <defs>
      <!-- Grid pattern (blueprint style) -->
      <pattern id="grid" width="60" height="60" patternUnits="userSpaceOnUse">
        <path 
          d="M 60 0 L 0 0 0 60" 
          fill="none" 
          stroke="currentColor" 
          stroke-width="0.3"
          opacity="0.15"
        />
      </pattern>
      
      <!-- Neural network nodes pattern -->
      <pattern id="nodes" width="120" height="120" patternUnits="userSpaceOnUse">
        <!-- Nodes -->
        <circle cx="20" cy="20" r="2" fill="currentColor" opacity="0.2" />
        <circle cx="100" cy="30" r="1.5" fill="currentColor" opacity="0.15" />
        <circle cx="60" cy="60" r="2.5" fill="currentColor" opacity="0.18" />
        <circle cx="30" cy="100" r="1.5" fill="currentColor" opacity="0.12" />
        <circle cx="90" cy="90" r="2" fill="currentColor" opacity="0.2" />
        
        <!-- Connections -->
        <line x1="20" y1="20" x2="60" y2="60" stroke="currentColor" stroke-width="0.3" opacity="0.1" />
        <line x1="100" y1="30" x2="60" y2="60" stroke="currentColor" stroke-width="0.3" opacity="0.1" />
        <line x1="60" y1="60" x2="30" y2="100" stroke="currentColor" stroke-width="0.3" opacity="0.08" />
        <line x1="60" y1="60" x2="90" y2="90" stroke="currentColor" stroke-width="0.3" opacity="0.08" />
        <line x1="90" y1="90" x2="100" y2="30" stroke="currentColor" stroke-width="0.3" opacity="0.06" />
      </pattern>

      <!-- Combined pattern -->
      <pattern id="combined" width="120" height="120" patternUnits="userSpaceOnUse">
        <rect width="120" height="120" fill="url(#grid)" />
        <rect width="120" height="120" fill="url(#nodes)" />
      </pattern>
    </defs>
    
    <rect width="100%" height="100%" fill="url(#combined)" />
  </svg>
</div>

<style>
  .tech-bg {
    position: fixed;
    top: 0;
    left: 0;
    width: 100%;
    height: 100%;
    z-index: -2;
    pointer-events: none;
    overflow: hidden;
  }

  .tech-pattern {
    width: 100%;
    height: 100%;
    color: #64748b; /* slate-500 */
    opacity: 0.025; /* Very subtle - about 2.5% */
  }
</style>
