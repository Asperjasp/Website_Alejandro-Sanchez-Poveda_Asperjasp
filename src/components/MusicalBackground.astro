---
// Componente de fondo musical estático con estilo de violín
---

<div class="musical-bg" aria-hidden="true">
  <svg class="musical-notes" xmlns="http://www.w3.org/2000/svg" preserveAspectRatio="xMidYMid slice">
    <defs>
      <!-- Patrón de pentagrama musical (estilizado - no es transcripción literal) -->
      <pattern
        id="staff-pattern"
        width="200"
        height="100"
        patternUnits="userSpaceOnUse"
      >
        <!-- Líneas del pentagrama (usar variable de color para mantener contraste) -->
          <!-- Líneas más finas y espaciadas para un efecto sutil de pentagrama -->
          <line x1="0" y1="22" x2="200" y2="22" stroke="var(--note-color)" stroke-width="0.5" />
          <line x1="0" y1="33" x2="200" y2="33" stroke="var(--note-color)" stroke-width="0.5" />
          <line x1="0" y1="44" x2="200" y2="44" stroke="var(--note-color)" stroke-width="0.5" />
          <line x1="0" y1="55" x2="200" y2="55" stroke="var(--note-color)" stroke-width="0.5" />
          <line x1="0" y1="66" x2="200" y2="66" stroke="var(--note-color)" stroke-width="0.5" />

          <!-- Clave de sol estilizada (decorativa) -->
          <path
            d="M 15 35 Q 15 25, 20 25 Q 25 25, 25 30 Q 25 35, 22 38 Q 20 40, 20 45 Q 20 50, 23 52 Q 26 54, 26 48 Q 26 42, 22 40"
            fill="none"
            stroke="var(--note-color)"
            stroke-width="1.6"
          />

          <!-- Motivo musical estilizado y abstracto: inspirador, no es una transcripción literal de una canción -->
          <!-- Las posiciones están pensadas para sugerir una melodía de violín sin reproducir una partitura protegida -->
          <ellipse cx="45" cy="38" rx="3" ry="2.6" fill="var(--note-color)" transform="rotate(-18 45 38)" />
          <line x1="48" y1="38" x2="48" y2="22" stroke="var(--note-color)" stroke-width="1.2" opacity="0.9" />

          <ellipse cx="82" cy="46" rx="3" ry="2.6" fill="var(--note-color)" transform="rotate(-14 82 46)" />
          <line x1="85" y1="46" x2="85" y2="30" stroke="var(--note-color)" stroke-width="1.2" opacity="0.85" />

          <ellipse cx="116" cy="33" rx="3" ry="2.6" fill="var(--note-color)" transform="rotate(-12 116 33)" />
          <line x1="119" y1="33" x2="119" y2="18" stroke="var(--note-color)" stroke-width="1.2" opacity="0.8" />

          <!-- Pares de corcheas sugeridas (conectadas de forma decorativa) -->
          <ellipse cx="142" cy="42" rx="3" ry="2.6" fill="var(--note-color)" transform="rotate(-15 142 42)" />
          <line x1="145" y1="42" x2="145" y2="26" stroke="var(--note-color)" stroke-width="1.2" opacity="0.75" />

          <ellipse cx="158" cy="36" rx="3" ry="2.6" fill="var(--note-color)" transform="rotate(-15 158 36)" />
          <line x1="161" y1="36" x2="161" y2="20" stroke="var(--note-color)" stroke-width="1.2" opacity="0.75" />

          <!-- Elementos decorativos (f-holes y arco ligeros) -->
          <path d="M 175 50 Q 177 54, 175 58" fill="none" stroke="var(--note-color)" stroke-width="1" opacity="0.12" />
          <path d="M 190 50 Q 188 54, 190 58" fill="none" stroke="var(--note-color)" stroke-width="1" opacity="0.12" />
      </pattern>
      
      <!-- Patrón secundario con elementos de violín -->
      <pattern
        id="violin-elements"
        width="300"
        height="150"
        patternUnits="userSpaceOnUse"
      >
        <!-- Arco de violín estilizado -->
        <!-- Arco de violín muy sutil, casi translúcido -->
        <path
          d="M 20 75 Q 80 72, 140 75"
          fill="none"
          stroke="#1f2937"
          stroke-width="1.4"
          opacity="0.12"
        />
        <line x1="20" y1="75" x2="25" y2="78" stroke="#334155" stroke-width="1" opacity="0.3" />
        <line x1="140" y1="75" x2="135" y2="78" stroke="#334155" stroke-width="1" opacity="0.3" />
        
        <!-- f-holes estilizados (aberturas del violín) -->
        <path
          d="M 252 62 Q 254 66, 252 70"
          fill="none"
          stroke="#1f2937"
          stroke-width="1.2"
          opacity="0.08"
        />
        <path
          d="M 266 62 Q 264 66, 266 70"
          fill="none"
          stroke="#1f2937"
          stroke-width="1.2"
          opacity="0.08"
        />
      </pattern>
    </defs>
    
    <!-- Aplicar patrones al fondo -->
    <!-- Capas: pentagrama y elementos de violín, con baja opacidad para no competir con el contenido -->
    <rect x="0" y="0" width="100%" height="100%" fill="url(#staff-pattern)" opacity="0.95" />
    <rect x="0" y="0" width="100%" height="100%" fill="url(#violin-elements)" opacity="0.9" />
  </svg>
</div>

<style>
  .musical-bg {
    position: fixed;
    top: 0;
    left: 0;
    width: 100%;
    height: 100%;
    z-index: -1;
    overflow: hidden;
    /* Fondo más apagado/desaturado para no competir con texto blanco */
    background: linear-gradient(135deg, #051119 0%, #0a1a22 35%, #08242b 100%);
    /* permitimos que el SVG use esta variable para las notas; muy sutil (blanco translúcido) */
    --note-color: rgba(255,255,255,0.085); /* ajustable en global.css */
  }
  
  .musical-notes {
    position: absolute;
    width: 100%;
    height: 100%;
    opacity: 0.06; /* menos intrusivo */
    mix-blend-mode: screen; /* ligero efecto para separar visualmente del fondo */
    filter: blur(0.4px) contrast(0.95);
    pointer-events: none;
  }
</style>
