---
// "Open Researcher" CTA component
// Friendly invitation for collaboration
---

<div class="open-researcher-cta">
  <div class="cta-content">
    <span class="cta-icon">📬</span>
    <p class="cta-text">
      Estoy abierto a <strong>colaboraciones</strong> y <strong>discusiones técnicas</strong> — 
      <a href="/contact" class="cta-link">escríbeme cuando quieras</a>.
    </p>
  </div>
</div>

<style>
  .open-researcher-cta {
    width: 100%;
    max-width: 700px;
    margin: 2rem auto;
    padding: 0 1rem;
  }

  .cta-content {
    display: flex;
    align-items: center;
    gap: 1rem;
    padding: 1.25rem 1.5rem;
    background: linear-gradient(135deg, rgba(16, 185, 129, 0.1) 0%, rgba(59, 130, 246, 0.1) 100%);
    border: 1px solid rgba(16, 185, 129, 0.25);
    border-radius: 0.75rem;
    transition: all 0.3s ease;
  }

  .cta-content:hover {
    border-color: rgba(16, 185, 129, 0.4);
    background: linear-gradient(135deg, rgba(16, 185, 129, 0.15) 0%, rgba(59, 130, 246, 0.15) 100%);
  }

  .cta-icon {
    font-size: 1.5rem;
    flex-shrink: 0;
  }

  .cta-text {
    color: #cbd5e1;
    font-size: 1rem;
    line-height: 1.6;
    margin: 0;
  }

  .cta-text strong {
    color: #6ee7b7;
    font-weight: 600;
  }

  .cta-link {
    color: #34d399 !important;
    text-decoration: underline;
    font-weight: 600;
    transition: color 0.2s ease;
  }

  .cta-link:hover {
    color: #6ee7b7 !important;
  }

  @media (max-width: 640px) {
    .cta-content {
      flex-direction: column;
      text-align: center;
      gap: 0.75rem;
      padding: 1rem;
    }

    .cta-text {
      font-size: 0.9rem;
    }
  }
</style>
